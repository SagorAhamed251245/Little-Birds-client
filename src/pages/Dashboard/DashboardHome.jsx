import Container from '../../component/Container/Container';
import SectionTitle from '../../component/SectionTitle/SectionTitle';

const DashboardHome = () => {
    return (
        <Container>
            <SectionTitle heading ={'WellCome TO Dashboard'} ></SectionTitle>
        </Container>
    );
};

export default DashboardHome;